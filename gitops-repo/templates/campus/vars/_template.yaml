apiVersion: v1
kind: ConfigMap
metadata:
  name: template-vars
  namespace: flux-data
data: {}
  # # Default for the cluster's floating ingress IP advertised by Cilium
  # # L2 announcements. Empty string here means "no L2 ingress IP set" —
  # # see templates/core/vars/_template.yaml for the same default and
  # # rationale. Override per-cluster in clusters/<type>/<name>/cluster-config.yaml.
  # CLUSTER_L2_IP_1: ""
